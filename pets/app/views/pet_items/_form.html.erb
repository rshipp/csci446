<%= form_for(@pet_item) do |f| %>
  <% if @pet_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pet_item.errors.count, "error") %> prohibited this pet_item from being saved:</h2>

      <ul>
      <% @pet_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pet_id %><br>
    <%= f.text_field :pet_id %>
  </div>
  <div class="field">
    <%= f.label :pet_list_id %><br>
    <%= f.text_field :pet_list_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
