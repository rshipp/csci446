<% if pet_item == @current_item %>
    <li id="current_item"><%= (Pet.where id: pet_item.pet_id).first.name %>
<% else %>
    <li><%= link_to (Pet.where id: pet_item.pet_id).first.name, pet_item.pet %>
<% end %>
