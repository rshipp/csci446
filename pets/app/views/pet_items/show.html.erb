<p id="notice"><%= notice %></p>

<p>
  <strong>Pet:</strong>
  <%= (Pet.where id: @pet_item.pet_id).first.name %>
</p>

<%= link_to 'Edit', edit_pet_item_path(@pet_item) %> |
<%= link_to 'Back', pet_items_path %>
